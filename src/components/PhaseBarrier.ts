import { defineComponent } from 'bitecs';

// Tag: hex is passable only when the Phase Shift ability is powered for that dimension.
// MovementSystem checks for this component during passability evaluation.
export const PhaseBarrier = defineComponent({});
