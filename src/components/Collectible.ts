import { defineComponent } from 'bitecs';

// Tag: marks a floor conduit as collectible. Renders as the ??? icon until
// CollectionSystem picks it up and adds the shape to the player's inventory.
export const Collectible = defineComponent({});
